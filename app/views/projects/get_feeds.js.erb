$('.feed-list').html("<%= escape_javascript(render :partial => 'feeds', :locals => {:feeds => @feeds} ).html_safe %>");
<% if @action == 'next'%>
$('.next').attr('data-offset','<%= @offset %>')
$('.previous').attr('data-offset','<%= @previous %>')
<% else %>
$('.next').attr('data-offset','<%= @next %>')
$('.previous').attr('data-offset','<%= @offset %>')
<% end %>
if($('.previous').attr('data-offset') == '0')
{
    $('.previous').addClass('hidden');
}
else
{
    $('.previous').removeClass('hidden');
}
if ( $('.feed-list ul li').length < 10 )
{
    $('.next').addClass('hidden');
}
else{
    $('.next').removeClass('hidden');
}



